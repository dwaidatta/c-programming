#include <stdio.h>
#include <math.h> // header file for math functions

int main(){
    double a = sqrt(9);
    double b = pow(2,4);

    int c = round(3.14);
    int d = ceil(3.14);
    int e = floor(3.99);

    double f = fabs(-100);
    double g = log(3);

    double i = sin(45);
    double j = cos(45);
    double k = tan(45);
    
}
